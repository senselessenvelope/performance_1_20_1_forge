// priority: 0

// -------------------
// ---[ LIBRARIES ]---
// -------------------

// OBJECTS
test.assertNotNull(global.objects, "global.objects not null")
test.assertNotNull(global.objects.meta, "global.objects.meta not null")
test.assertNotNull(global.objects.entity, "global.objects.entity not null")
test.assertNotNull(global.objects.entity.projectile, "global.objects.entity.projectile not null")
test.assertNotNull(global.objects.player, "global.objects.player not null")
test.assertNotNull(global.objects.item, "global.objects.item not null")

// UTILS
test.assertNotNull(global.utils, "global.utils not null")
test.assertNotNull(global.utils.entity, "global.utils.entity not null")
test.assertNotNull(global.utils.entity.projectile, "global.utils.entity.projectile not null")
test.assertNotNull(global.utils.player, "global.utils.player not null")
test.assertNotNull(global.utils.item, "global.utils.item not null")

